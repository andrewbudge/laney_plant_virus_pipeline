# Nextflow runtime state -- regenerated every run, never shared between machines
.nextflow/
.nextflow.log*
work/
results/
report.html
timeline.html
trace.txt
dag.html

# Container images
*.sif
*.img

# Data never belongs in the repo: reads, references and indexes live under
# --data_dir / --db on whatever machine is running.
*.fastq
*.fastq.gz
*.fq
*.fq.gz
*.fasta
*.fa
*.fna
*.faa

# OS / editor
.DS_Store
*.swp
*~

# Iteration notes -- rewritten once at release; pipeline is still moving
README.md
AGENTS.md
